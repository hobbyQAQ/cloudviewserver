<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cloudviewserver.dao.PhotomapperDao">
    <resultMap id="PhotomapperMapper" type="com.example.cloudviewserver.entity.Photomapper">
        <result property="pid" column="pid" jdbcType="INTEGER"/>
        <result property="uid" column="uid" jdbcType="INTEGER"/>
    </resultMap>

    <!--新增所有列-->
    <insert id="insert" parameterType="Photomapper" >
        insert into cloudview.photomapper(pid, uid)
        values (#{pid},#{uid})
    </insert>
</mapper>